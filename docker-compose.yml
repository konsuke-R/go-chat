version: "3.8"
services:
  app:
    build: .
    volumes:
      - .:/app
    ports:
      - "8080:8080"
    command: ["go", "run", "main.go"]
    depends_on:
      - redis
  redis:
    image: "redis:alpine"
    ports:
      - "6379:6379"
    tty: true